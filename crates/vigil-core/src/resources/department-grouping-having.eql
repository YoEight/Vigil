FROM e IN events
GROUP BY e.data.department HAVING AVG(e.data.salary) > 90000
ORDER BY AVG(e.data.salary) DESC
PROJECT INTO {
	department: UNIQUE(e.data.department),
	employeeCount: COUNT(),
	avgSalary: AVG(e.data.salary)
}
